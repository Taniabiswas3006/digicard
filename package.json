{
  "name": "kathryn_card_monorepo",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix Backend",
    "dev": "npm run dev --prefix frontend",
    "server": "npm run start --prefix Backend",
    "build": "npm run build --prefix frontend"
  }
}
